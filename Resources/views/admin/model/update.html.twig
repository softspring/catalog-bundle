{% trans_default_domain 'sfs_catalog' %}
{% extends '@SfsCatalog/admin_layout.html.twig' %}
{% block title %}{{ 'admin_models.update.page_title'|trans }}{% endblock %}

{% block content %}
    <div class="d-flex align-items-center justify-content-between mb-4">
        <h1 class="h3">Update model</h1>
    </div>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_rest(form) }}
        <div class="d-flex justify-content-end align-items-center">
            <a href="{{ url('sfs_catalog_admin_models_list') }}" class="btn btn-outline-secondary ml-3">{{ 'admin_models.update.actions.cancel.button'|trans }}</a>
            <a href="{{ url('sfs_catalog_admin_models_delete', {'entity':entity}) }}" class="btn btn-danger ml-3">{{ 'admin_models.update.actions.delete.button'|trans }}</a>
            <input type="submit" value="{{ 'admin_models.update.actions.update.button'|trans }}" class="btn btn-primary ml-3" />
        </div>
    {{ form_end(form) }}
{% endblock content %}