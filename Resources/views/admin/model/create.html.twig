{% trans_default_domain 'sfs_catalog' %}
{% extends '@SfsCatalog/admin_layout.html.twig' %}
{% block title %}{{ 'admin_models.create.page_title'|trans }} {% endblock %}

{% block breadcrums_content %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ url('sfs_catalog_admin_models_list') }}">{{ 'admin_models.list.breadcrumb'|trans }}</a></li>
    <li class="breadcrumb-item active" aria-current="page">{{ 'admin_models.create.breadcrumb'|trans }}</li>
{% endblock breadcrums_content %}

{% block content %}
    {% block content_header %}
        <div class="d-flex align-items-center justify-content-between mb-4">
            <h1 class="h3">{{ 'admin_models.create.title'|trans }}</h1>
        </div>
    {% endblock content_header %}

    {% block form %}
        {{ form_start(form) }}
        {% include '@SfsCatalog/admin/model/_form.html.twig' %}
        <div class="d-flex justify-content-end align-items-center">
            <a href="{{ url('sfs_catalog_admin_models_list') }}" class="btn btn-outline-secondary ml-3">{{ 'admin_models.create.actions.cancel.button'|trans }}</a>
            <input type="submit" value="{{ 'admin_models.create.actions.create.button'|trans }}" class="btn btn-primary ml-3" />
        </div>
        {{ form_end(form) }}
    {% endblock form %}
{% endblock content %}